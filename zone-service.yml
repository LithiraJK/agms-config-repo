server:
  port: 8081
  servlet:
    context-path: /zone-service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_zone_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
    defaultZone: http://localhost:8761/eureka/
